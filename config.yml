site:
    title: "行雲小站"
    subtitle: "Say hello to you!"
    limit: 10
    theme: theme
    lang: zh
    url: "http://w568w.ml"
    comment: username
    logo: "-/images/avatar.gif"
    # link: "{category}/{year}/{month}/{day}/{title}.html"
    # root: "/blog"

authors:
    w568w:
        name: "w568w"
        intro: "夢想改變自己的瘋子。"
        avatar: "-/images/avatar.gif"

build:
    output: "/"
    port: 8000
    # Copied files to public folder when build
    copy:
        - "source/images"
    # Excuted command when use 'ink publish'
    publish: |
        git add . -A
        git commit -m "update"
        git push origin
